#include <string.h>
#include <stdio.h>
#include <stdlib.h>
#include "gad.h"

using namespace Gad;

int MyRT::goElse(MyRT* rt,char* p[],int nv) { 
  /*
  rt->setIdent(rt->ident-2); int i = 0;
  if((rt->gen == GO) || (rt->gen == RUST)) rt->to(rt->ident),rt->to("} else {\n"); 
  if((rt->gen == MOJO) || (rt->gen == PYTHON)) rt->to(rt->ident),rt->to("else:\n"); 
  rt->setIdent(rt->ident+2);
  */
  return 0;
}
